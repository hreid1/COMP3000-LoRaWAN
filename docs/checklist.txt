Things to do
- Add functionality to profile, settings and logout
- Add login page
	- Configure code so that it fetches information regarding the user who logged in
- Clean up UI/UX structure
	- AI Models
	- Devices display -> Devices page
	- Device list display -> dashboard
- Add customisation for graphs (e.g. a user can pick if they want to see num of packets per node against time)
- Finish off pages
	- Admin
	- Anomaly
	- Profile
	- Settings -> change password, email, username, has to get verified before change is made to backend
- Encryption/secure stuff -> salted passwords
- Convert devices information to fetch from a dataset not using an artificial dataset
- Add a way to restructure the dashboard
- When model is ran -> packet column 'is_anomalous' is changed to true for packets which are flagged to be anomalous, alongside entry inside Anomaly table 
- Add logs for actions e.g. running a file against a model, adding a model, removing nodes packets, models etc
- Add scroll wheel functionality to map in mapbox
- Include tests
	- Unit Tests
	- Integration Tests
	- Canary Tests
- After running a model, a popup should appear telling the user the summary of the ran e.g. a modal menu, including name of file, performance metrics,
- For explaining why a packet was flagged either use:
	- SHAP (Shapley Additive Explanations) -> values for tree-based machine learning models
	- Z-scores calculates how deviant a value is from normal 
- Add a load more button option which loads more devices, anomalies, packets from the dataset
	- Devices, Anomalies, 
- Add options to run packets against a combination of models (e.g. isolation forest and local outlier factor) as well as being able to individually run specific ai models
- Deployment checklist
	- Authorisation
	- Validating and sanitising all user inputs
	- CORS policy 
	- Rate limiting
	- Password reset links expire
	- Unexpected error handling frontend
	- Indexing on common DB queries
	- Logging
	- Alerts 
	- Rollback deployment if it goes wrong
		- blue green deployment strategy
- UI/UX improvements
	- Home dashboard
		- Jammer confidence score not just 'healthy'
			- Replace with jammer probability score
		- Fix SNR char x-axis
			- SNR displays everything at 01:00, should be last 1hr, 2hrs etc
		- Annotate anomaly events on time-series charts
			- Correlate the two graphs
	- Anomaly 
		- Replace placeholders with real panels
			- Severity
			- Anomaly frequency over time
			- Top affected device list
		- Severity classification on each anomaly card
			- Use the anomaly score
		- Filters: date range, model, severity, device
		- Dismiss, acknowledge action with aduit trail
			- Dismissed anomalies get moved to a separate tab
	- AI-info page
		- Show model performance metrics, not just run history
		- Feature importance visualisation
			- Showing which features contribute mostly to anomaly detection
		- Threshold tuning UI in model management
			- Slider for contamination factor for isolation forest or n_neighbors for LOF
	- Devices page
		- Map should show anomaly markers and device pins
			- Different colours to show where jammer attacks are generalising
		- Device detail drawer with signal history
			- Clicking on a device on the map should open a side drawer showing the device's RSSI'SNR trends, anomaly trends, last seen time
	- Logs page
		- Structured log viewer with filtering and severity
			- Timestamp, log level (INFO/WARN/ERROR), source (deviceID, model name, system) and message
			- Add text search and level filters
- The network-level score should be `max()` of per-device scores, not the mean
- Consider adding disagreements between models (e.g. this packet has been flagged by IF, but not by LOF, why?)
- add @media to css files for different screen shapes (e.g. mobile, laptop)
- Sort some of the UI/UX components 
	- Pop up after model has ran
	- For view anomaly function, add close marker top right and clarify dismiss as I ACKNOWLEDGE ANOMALY
- Add pagination for device list and anomaly list
- Filter list for logs 
	- Date range
	- Log type
		- model being ran
			- User ID, datetime, 
		- File being uploaded
		- Devices/Nodes being added
		- Logging in, logging out
		- Changing details about profile
		- Errors
			- What type of error (e.g 404, 400, 500?) 
		- 
	- Severity
		- success, info, warning, error
		- OR critical, high, medium, low
- Recent activity + Announcements need doing
- Add button for dismissing announcements/recent-activity
- Rename AI-info to something else
- Use a blue-green deployment
- Different fonts within the page
- Need to figure out how the device list filtering works
- Fix graph on devices page